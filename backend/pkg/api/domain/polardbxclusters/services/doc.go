package services

// Package services provides orchestration for the logical cluster domain (idempotent steps/flows).
// It aligns with the operator's step philosophy, splitting into precheck/execute/finalize phases.
