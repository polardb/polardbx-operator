apiVersion: v1
kind: Secret
metadata:
  name: backup-oss-credentials
  namespace: default
type: Opaque
stringData:
  accessKeyId: YOUR_ALICLOUD_ACCESS_KEY_ID
  accessKeySecret: YOUR_ALICLOUD_ACCESS_KEY_SECRET
  region: cn-hangzhou
  endpoint: https://oss-cn-hangzhou.aliyuncs.com
